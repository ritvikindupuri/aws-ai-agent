--- src/components/ChatInterface.tsx
+++ src/components/ChatInterface.tsx
@@ -73,6 +73,8 @@
     if (!trimmed || isLoading || !credentials?.session) return;
     setInput("");

+    // Let the chat hook handle optimistic UI by passing the content immediately
+    // We can change how createConversation interacts with sendMessage
     let convId = currentConvId;

     // Create a new conversation if none is active
